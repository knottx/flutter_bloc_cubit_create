name: flutter_bloc_create_page
description: "Flutter Bloc/Cubit Create"
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  archive: ^4.0.7
  cupertino_icons: ^1.0.8
  recase: ^4.1.0
  syntax_highlight: ^0.5.0
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  fonts:
    - family: Consolas
      fonts:
        - asset: assets/fonts/Consolas.ttf
          weight: 400
